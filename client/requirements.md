## Packages
(none needed - existing packages are sufficient)

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  body: ["var(--font-sans)"],
}

Images are imported statically from @assets as requested.
Framer Motion is used for the "Magnify" layout animations.
Next-themes is used for Dark/Light mode toggling.
